{
  "name": "blog_posting",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "dev": "npm run dev:api & npm run dev:web"
  }
}
